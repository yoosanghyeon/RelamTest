<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="yoosanghyeon.relamtest">

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".read.ReadActivitiy"
            android:label="@string/title_activity_read_activitiy"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".wirte.WriteActivity"
            android:label="@string/title_activity_wirte"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".IntroExampleActivity" />
        <activity android:name=".search.SearchActivity"></activity>
    </application>

</manifest>